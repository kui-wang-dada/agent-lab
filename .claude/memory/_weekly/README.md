# _weekly/ —— 周巡 digest

curator 周巡（`scripts/curator-prompt.txt` 步骤 ⑥）每周产出 `<ISO周>-digest.md`，逐条列出**本周自动落了什么**（类别 / 落点 / 置信度 / 一句话）+ 高风险候选清单。

用途：Kevin 事后**审阅 + 否决**经验回流引擎本周的自动沉淀。否决方式 = `git revert` 对应改动（自动落的都是 git-tracked 文件）。

> 这是"分层自治 — 强默认 + 可否决"里"可否决"那一环的落点。
